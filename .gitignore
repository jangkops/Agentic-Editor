node_modules/
ai_engine/.venv/
ai_engine/__pycache__/
.DS_Store
*.pyc
dist/
# electron-builder 리소스: 디렉터리 전체 제외 대신 산출물만 제외하고, 서명·공증에 필요한 entitlements 는 추적한다
# (CI fresh checkout 에 없으면 서명 빌드가 실패 — 분석 원장 #35, 2026-09-17).
build/*
!build/entitlements.mac.plist
스크린샷 복원/
스크린샷_resized/
node_modules
.rag_cache/

# GCP service account keys — NEVER commit
*.gcp-key.json
*-vertex-poc-key*.json
gcp-credentials/
*service-account*.json

# Build / packaging artifacts
dist_electron/
ai_engine_dist/
dist-python/
build-python-work/
*.spec.bak

# Generated runtime artifacts (decks, docs, audit/render outputs)
.generated/
coverage/
scripts/_audit_out/
scripts/_render_out/

# Generated demo media at repo root
/*.pptx
/*.pdf
/*.docx
/*.xlsx
/agentic-editor-architecture.png

# Temp / probe / scratch artifacts (never commit)
.ae_*
.tmp_patch*
_tmp*
scripts/_*_probe.js
**/_rfb_temp.js

# deep-research-engine: 검색 제공자 자격증명 (절대 커밋 금지)
# API 키는 env/시크릿 런타임 주입만, 파일 저장 금지 (steering security / 요구사항 11)
.env.research
**/research_secrets*
*research*.key
*research*.secret
