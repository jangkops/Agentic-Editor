# Bedrock Gateway Integration Steering  ## Endpoints - POST /process        → non-streaming - POST /streamprocess  → SSE streaming - POST /embed          → vectorization  ## Request (mandatory fields) requestid (UUID v4), requestdatetime, appid, userid, costcenter, provider: "amazon-bedrock", requestbody.model, requestbody.body.messages  ## Models - anthropic.claude-3-opus-20240229-v1:0     → Planner, Evaluator - anthropic.claude-3-5-sonnet-20241022-v2:0 → Generator  ## Errors - 403 → QuotaExceededError → show ApprovalRequestDialog - 422 → log + surface to user - 500 → retry 3x exponential backoff (1s, 2s, 4s) - timeout: 120s  ## Security - appid and apitoken loaded from userData/settings/settings.json at runtime - NEVER log apitoken in full — mask as apitoken.substring(0,4) + '****' - NEVER store credentials (accessKeyId, secretAccessKey) in settings.json