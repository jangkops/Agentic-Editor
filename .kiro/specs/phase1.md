# Phase 1 Spec  ## Goal Runnable Electron app: SSO login → Monaco editor visible → Python backend healthy  ## Acceptance Criteria - npm run dev starts Python (8765) + Electron with no errors - /health responds < 2s - SSO overlay on first launch with profile list from ~/.aws/config - After login: overlay hides, Monaco editor renders with vs-dark theme - userData/settings/settings.json written after login - No .ts files, no React imports, no SQLite anywhere - Playwright test confirms: app launches, SSO dialog visible, Monaco loads  ## Files in Order 1→14 (see Implementation Steps)